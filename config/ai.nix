{
  plugins.copilot-lua = {
    enable = true;
    suggestion = { enabled = false; };
    panel = { enabled = false; };
  };
  plugins.copilot-chat = {
    enable = true;
  };
  plugins.copilot-cmp = {
    enable = true;
  };
}
